/** GPA deducted from the optional (4th) subject GPA to produce the bonus. */
export const OPTIONAL_DEDUCTION = 2.0;

/** Denominator of the final GPA formula. Must stay 6. */
export const FINAL_GPA_DIVISOR = 6;
